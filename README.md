# tcpdump-wifi-scanner
A simple tcpdump based wifi scanner for surrounding devices. Should work on any operating system supporting tcpdump.
Uses d3js to render surrounding nodes.

## Usage
````./run_server.sh````
